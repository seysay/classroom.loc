{% extends 'base.html.twig' %}

{% block title %}ClassRoom index{% endblock %}

{% block body %}
    <h1>ClassRoom index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Class</th>
                <th>Created</th>
                <th>Active</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for class_room in class_rooms %}
            <tr>
                <td>{{ class_room.id }}</td>
                <td>{{ class_room.class }}</td>
                <td>{{ class_room.created ? class_room.created|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ class_room.active ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('class_room_show', {'id': class_room.id}) }}">show</a>
                    <a href="{{ path('class_room_edit', {'id': class_room.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('class_room_new') }}">Create new</a>
{% endblock %}
